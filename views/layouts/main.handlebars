<!DOCTYPE html>
<html lang="en">
	<head>
	    <meta charset="utf-8">
	    <title>{{title}}</title>
	</head>
	<body>
		<nav>
			<a href = "/login_signup">{{LOGIN_LINK}}</a>
 			<a href = "/logout">{{LOGOUT_LINK}}</a>
 		    <p>{{USERNAME}}</p>
		</nav>
		<h1>{{title}}</h1> 
		{{{body}}}
	</body>
</html>

