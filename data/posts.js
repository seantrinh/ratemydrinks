//Post modules
modules.export = {
	async postReview(params) { //update these
		//given params like
		//	user posting
		//	content
		//	etc
		//create the post in the database
	},
	async deleteReview(pid) {
		//given a pid, delete the review in the database
	}
}
